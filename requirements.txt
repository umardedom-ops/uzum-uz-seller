# --- Bot & backend ---
aiogram>=3.13
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.9   # Click webhook form-urlencoded uchun

# --- Database ---
sqlalchemy[asyncio]>=2.0.36
alembic>=1.14
asyncpg>=0.30            # PostgreSQL (production)
aiosqlite>=0.20          # SQLite (lokal dev / testlar)

# --- Scheduler & HTTP ---
apscheduler>=3.11
httpx>=0.27

# --- Hujjatlar ---
openpyxl>=3.1            # Excel hisobot
python-docx>=1.1         # Pretenziya
reportlab>=4.0           # PDF hisobot
# DIQQAT: PDF'da kirill matn uchun TrueType shrift kerak.
# Linux serverda: apt install fonts-dejavu-core

# --- Config & security ---
pydantic>=2.9
pydantic-settings>=2.5
cryptography>=43         # Fernet shifrlash

# --- Dev / test ---
pytest>=8.3
pytest-asyncio>=0.24
ruff>=0.8
